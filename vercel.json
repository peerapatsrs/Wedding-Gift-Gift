{
	"version": 2,
	"buildCommand": "npm install --force && npm run build",
	"devCommand": "npm run dev",
	"installCommand": "npm install --force",
	"framework": "sveltekit",
	"nodeVersion": "20.x",
	"builds": [
		{
			"src": "package.json",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/"
		}
	]
}
